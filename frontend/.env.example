# Local dev does not require a frontend API variable.
# Next.js proxies /api/* to the backend via rewrites (see next.config.ts).
# In production, the frontend falls back to the deployed backend origin.
# Set this variable only if you want to override that default.
# NEXT_PUBLIC_API_BASE_URL="https://collector-api-f9xu.onrender.com"
